function alphabetPosition(text) {
  const LetterValues = [a = 1, b = 2, c = 3];

/*   text.forEach(letter => {
      console.log(LetterValues[letter])
  }); */
  /* let posistionString = ""
*/
      for (let i = 0; i < text.length; i++) {
        /* const meep = LetterValues.filter(letter => letter === text[i])
        console.log(meep) */
        const helper = text[i]

          
      }
 
  //return posistionString;
}

//const result = alphabetPosition("The sunset sets at twelve o' clock.");
const result = alphabetPosition("aaccbb");

console.log(result);
